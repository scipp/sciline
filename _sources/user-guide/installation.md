# Installation

To install Sciline and all of its dependencies, use

`````{tab-set}
````{tab-item} pip
```sh
pip install sciline
```
````
````{tab-item} conda
```sh
conda install -c conda-forge -c scipp sciline
```
````
`````
